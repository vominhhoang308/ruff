name: bench-ruff-depot-cold
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: depot-ubuntu-24.04-4
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo build --release 
